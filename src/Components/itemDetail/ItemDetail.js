import React from "react";

export const ItemDetail = ({ producto }) => {
  return (
    <> 
    itemDetail
    </>
  );
};

const styles = {
  container: {
    display: "flex",
    flexDirection: "row",
    alignItems: "center",
  },
  Infocontainer:{
    display: "flex",
    flexDirection: "column",
  },
  img:{
    maxWidth: "20%"
  }
};